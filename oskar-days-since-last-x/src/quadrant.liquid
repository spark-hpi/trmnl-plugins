{% if mode == 'meme' %}
<div class="layout layout--col layout--center layout--stretch gap--small">
  <img class="image image-dither" src="{{ meme_image_url }}">
  {% if meme_message != blank %}<span class="label label--xsmall text--center">{{ meme_message }}</span>{% endif %}
</div>
{% else %}
<div class="layout layout--col layout--center gap">
  <span class="label label--small">It has been</span>
  <span class="value value--xxxlarge" data-fit-value="true">{{ display_days }}</span>
  <span class="label label--small">days since {{ display_label }}</span>
</div>
{% endif %}

<div class="title_bar">
  {{ title_bar_icon }}
  <span class="title">{{ trmnl.plugin_settings.instance_name }}</span>
  <span class="instance">{% if mode == 'meme' %}Meme of the Week{% else %}Days Since Last X{% endif %}</span>
</div>

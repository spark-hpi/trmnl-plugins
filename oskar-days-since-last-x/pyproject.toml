[project]
name = "oskar-days-since-last-x"
version = "0.1.0"
description = "TRMNL plugin counting the days since a recurring event"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.34.2",
]

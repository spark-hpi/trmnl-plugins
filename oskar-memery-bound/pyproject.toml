[project]
name = "oskar-memery-bound"
version = "0.1.0"
description = "TRMNL plugin that shows the most-reacted Telegram image as the meme of the week"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.14.1",
    "requests>=2.34.2",
    "telethon>=1.44.0",
]
